{
  "version": 2,
  "buildCommand": "pnpm --filter @workspace/real-estate run build",
  "outputDirectory": "artifacts/real-estate/dist",
  "installCommand": "pnpm install",
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}